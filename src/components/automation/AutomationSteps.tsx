import React from 'react';
import './AutomationSteps.css';

interface AutomationStepsProps {
    projectId: string;
}

const AutomationSteps: React.FC<AutomationStepsProps> = ({ projectId }) => {
    return (
        <div>Automation Steps</div>
    );
};

export default AutomationSteps;
