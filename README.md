
  # Create Page in Angular

  This is a code bundle for Create Page in Angular. The original project is available at https://www.figma.com/design/0JCHONSTGYtemM84lKZbFu/Create-Page-in-Angular.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  